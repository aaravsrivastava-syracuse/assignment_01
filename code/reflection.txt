I learned a lot of things during this lessonone thing I learned was how to check inputs before dividing, using the split_evenly check when people <=0 rauses ValueError because zero or negative people cannot share a bill. I also learned that the debgger can be used to trace wrong values in a lot of ways, I used it as a breakpoint on the console's per_person calculation to inspect the total, subtotal and people. I also learned how one function can call another instead of repeating code, I struggled out knowing what value to pass into split_evenly I fixed it by learning to use what a breakpoint was and checking the values of the subtotal.
